#include <stdio.h>
#include "main.h"
/**
 * _islowe - Entry point
 * Description: checks if a given letter is lowercase
 * Return: 0 or 1
 */
int _islower(int c)
{
	int c;

	if (int islower_l(int c, locale_t locale))
	{
		return (1);
	}else
	{
		return (0);

	}

}
