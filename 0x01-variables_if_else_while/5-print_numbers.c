#include <stdio.h>
/**
 * main - print the numbers 
 * Description: using the main function
 * tis prints out the single digits
 * Return: 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%i\n", i);
	}
	return (0);
}
